We ran @desloppify on banteg/crimson — a reverse-engineered Crimsonland in Python + raylib. 132K lines, 727 files.

Strict score: 77.4/100. That's genuinely good.

Here's what the scanner found 🧵

The biggest drag: test health at 12.3%. 325 production modules without coverage. The network runtime (lockstep + rollback) is a complex stateful beast with limited tests. When your netcode is doing rollback prediction, maybe test it?

Found a sneaky anti-pattern: cast(RelayUdpTransport, None) — typed as non-optional, actually None at runtime. The type checker sees no problem. Your production code will.

loop_view.py: GLSL shaders, gamma correction, view dispatch, and screen transitions — all in one file. It's the "I'll just add one more thing" hall of fame.

Also: preserve_bugs=False threaded through 3 different config types. The creatures module literally says "This package will grow as we port creature_* logic from the original binary." Respect the honesty.

But honestly? This is a strong codebase.
- Import linter enforcing architectural boundaries
- 96.5% security score
- Zero AI-generated debt (all hand-written)
- msgspec structs everywhere, modern Python 3.13+ tooling

77.4/100 strict — banteg ships clean code. The main fix is just: write more tests.

Full report + methodology: github.com/peteromallet/desloppify
Run desloppify on YOUR repo: pip install desloppify
